#Asigna permisos básicos para que puedas acceder y crear o editar cada archivo y directorio.
chmod 777 examPermisosAvanzado

#Tarea 2: Permisos y Contenido en privateFiles
chmod 600 privateFiles
#Solo el dueño (estudiante) puede leer y escribir.
#Nadie más puede ni siquiera leer el contenido.
chmod 777 privateFiles/
Rellenar:
#credentials.txt: Almacenar al menos dos líneas con salida de un comando, por ejemplo whoami o uname -r.
whoami >> credentials.txt
cat credentials.txt 
giovv
#configMain.cfg: Añadir información de configuración (ficticia o real). ejm:
at configMain.cfg 
echo "Configuración Principal
PORT=8080
MODE=production"

#Tarea 3: Directorio Público
#Requisitos:
#Cualquier usuario puede crear, leer y escribir archivos dentro de publicZone.
chmod 777 runPublic.sh
#El script runPublic.sh debe ser ejecutable por todos.
chmod 111 runPublic.sh 
#Rellenar:
#readMeInfo.md: Al menos 2 líneas explicando su uso.
cat readMeInfo.md 
Es un archivo que sirve para que trata tu proyecto, explicacion y uso 
#runPublic.sh: Pequeño script de ejemplo, p. ej.:
!/bin/bash
echo "Hola, este script es público, mi nombre es $nombreEstudiante ."


#Tarea 4: Directorio de Grupo

#Requisitos:
#El propietario (tú) puede leer, escribir y ejecutar sharedScript.sh.
#Otros miembros del grupo pueden leer y escribir sharedScript.sh, pero no ejecutarlo.
chmod 760 sharedScript.sh 

#Rellenar:
#groupDoc.txt: al menos 2 líneas describiendo el trabajo en grupo.
cat groupDoc.txt 
Este es un trabajo en grupo xdxd
#sharedScript.sh: un script sencillo:
cat sharedScript.sh 
!/bin/bash
echo "Script compartido para el grupo teamWork"

#Tarea 5: Administración de Logs

#Requisitos:

#Solo el propietario puede leer y escribir estos archivos
#Nadie más puede acceder.
chmod 600 mainLog.log 
chmod 600 dailyLog.log 
#Rellenar:
#mainLog.log: al menos 2 líneas con, por ejemplo, la salida de date o eventos ficticios:
cat mainLog.log 

[INFO] Sistema iniciado
[WARN] Lectura de sensor tardía
#dailyLog.log: 1 línea con la salida de date.
cat dailyLog.log 
mar 11 mar 2025 12:32:30 -04

#Tarea 6: Scripts de Administración

#cleanSystem.sh:
#Ejecutable solo por el propietario.
#Nadie más puede leer ni escribir en él.

#findData.sh:
#Ejecutable por el propietario y el grupo.
#Otros usuarios pueden leer el archivo, pero no ejecutarlo.

#Rellenar:

cleanSystem.sh: un script que muestre un mensaje, p. ej.:
  #!/bin/bash
  echo "Limpiando /tmp y procesos inactivos"
findData.sh: un script que busque ficheros o muestre algo, p. ej.:
#!/bin/bash
echo "Buscando data en examFinalAvanzado..."
ls examFinalAvanzado
