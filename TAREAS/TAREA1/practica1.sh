#!/bin/bash
#(pwd)nos muestra el directorio
pwd
# (cd)accede a la carpeta /tmp
cd /tmp
# (ls) lista su contenido
ls
#(cd~) nos vuelve al directorio
cd ~
#(mkdir -p) crea carpetas en este caso se va a llamar practica_shell 
mkdir -p practica_shell
#(cd) accede a carpetas en este caso a practica_shell
cd practica_shell
#(pwd) Nos muestra el directorio
pwd

